# Irodori-TTS デスクトップアプリ

音声合成と LoRA 学習をひとつのウィンドウで行うアプリです。

---

## 必要なもの

| 項目 | 条件 |
|-|-|
| OS | Windows 10 / 11 |
| Python | **3.10**（[3.10.11 のダウンロード](https://www.python.org/downloads/release/python-31011/)） |
| Node.js | LTS 版（[nodejs.org](https://nodejs.org/en/download)） |
| Git | [Git for Windows](https://git-scm.com/install/windows)（一部のライブラリの取得に使います） |
| GPU | NVIDIA 製・VRAM 8GB 以上を推奨（AMD は試験対応、GPU 無しでも CPU で動作） |
| 空き容量 | **30GB 以上**（Python 環境 5.7GB ＋ モデル 17GB ＋ データセットと LoRA の置き場） |

Python を入れるとき、インストーラの **「Add python.exe to PATH」** に必ずチェックを入れてください。

> **Python は通常 3.10 を使ってください。**
> 3.11 以降では音声処理ライブラリ（sentencepiece）の導入に失敗します。
>
> ただし **AMD (Radeon) を使う場合だけ 3.12** が必要です。AMD が配る
> Windows 用 PyTorch が 3.12 でしか配布されていないためで、`setup.bat` が
> GPU を見て自動で使い分けます。
>
> 3.12 などが既に入っていても構いません。必要な版を追加で入れれば
> `setup.bat` が自動で選びます。

---

## 使い始めるまで

### 1. セットアップ

`setup.bat` をダブルクリックします。

次の3つを自動で構築します。

|工程|内容|
|-|-|
|1|Electron（アプリの外枠）|
|2|Python 環境|
|3|音声モデル（約 17GB）|

初回は 30〜60 分ほどかかります。回線速度によります。
最後に「セットアップが終わりました」と出れば成功です。

途中で止めてしまっても、もう一度実行すれば続きから再開します。
取得済みのものは飛ばすので、2回目以降は速く終わります。

以前の版から更新した場合、`Irodori-TTS-v4` フォルダ（約 5GB）は使われなく
なります。削除して構いません。`setup.bat` が見つけたときは知らせますが、
自動では消しません。

**更新したときは `setup.bat` をもう一度実行してください。**
版によって必要なファイルが増えることがあります。足りないまま起動すると、
その版で追加されたモデルだけが生成に失敗します。取得済みのものは飛ばすので、
不足分だけが短時間で入ります。

**「セットアップを中断しました」と出た場合は、そのまま使わないでください。**
必要なものが揃っていない状態です。表示されたエラーに対処してから、
もう一度 `setup.bat` を実行してください。

### 2. 起動

`起動.bat` をダブルクリックします。

モデルはセットアップ時に取得済みなので、そのまま使い始められます。

---

## タブの説明

| タブ | できること |
|-|-|
| Synthesize | テキストから音声を作る |
| Dataset | 音声からデータセットを作る |
| Train | LoRA を学習する |
| 朗読 | テキストファイルを読み上げる |
| 青空文庫 | 著作権切れ作品を取得して朗読する |
| LoRAマージ | 複数の LoRA を混ぜる |
| 辞書 | 読み間違いを直す |

---

## 青空文庫タブの使い方

作品の探し方は 2 通りあります。

**作品名・著者名で探す**
上段に入力して「🔍 検索」。初回だけ作品一覧（約 2MB）を取得するので
30 秒ほどかかります。2 回目以降は手元のデータで即座に検索できます。

**図書カードの URL から開く**
青空文庫のサイトで作品ページを開き、その URL を下段に貼って
「この作品を開く」。次の形式に対応しています。

```
https://www.aozora.gr.jp/cards/000148/card789.html
```

どちらの場合も「📥 テキスト保存」を押すと `APP\novels` に保存され、
そのまま「📖 朗読タブで開く」で読み上げに渡せます。
ルビや注記は自動で取り除かれます。

収録は**著作権が切れた作品のみ**です。存続中の作品は一覧に出ません。

---

## 生成のしかた

参照音声（サンプルボイス）と LoRA の組み合わせで 4 通りあります。

1. サンプルボイスのみ
2. サンプルボイス ＋ 話し方 LoRA
3. サンプルボイス ＋ 声質 LoRA
4. 完パケ LoRA のみ（参照音声なし）

LoRA が声質を持つかどうかはアプリが自動で判定します。
話し方のみの LoRA を選んでサンプルボイスが未選択のときは
「サンプルボイスを選択してください」と表示され、選ぶと生成できます。

---

## LoRA の学習プリセット

| 表示 | 声質を学ぶ | 話し方を学ぶ |
|-|-|-|
| 話者＋話し方（既定） | ○ | ○ |
| 全体 | ○ | ○ |
| 話者のみ | ○ | — |
| 話し方のみ | — | ○ |

迷ったら既定の「話者＋話し方」を使ってください。

学習データは**量より質**です。
ノイズのない 20 分の音声が、雑多な 70 分より良い結果になることがあります。

---

## エンジンの違い

v4.1 / v4 / v3 / v2 と VoiceDesign 2種の計6モデルを切り替えられます。
すべて同じ Python 環境で動きます。

モデルごとの推奨値は次のとおりです。アプリ側で自動的に設定されます。

| 項目 | v4.1 / v4 | v2 / v3 |
|-|-|-|
| cfg-scale-text | 3.0 | 2.0 |
| cfg-scale-speaker | 5.0 | 3.0 |
| duration-scale | 1.0 | 1.0（v2 系は効きません） |

duration-scale が効くのは尺を予測するモデルだけです。v2 と v2 VoiceDesign は
固定 30 秒で学習されていて予測を持たないため、動かしても出力が変わりません
（アプリ側でスライダーが無効になります）。

**発話速度の注意**

- 記入欄の右下に出る予測秒数は LoRA 使用時の実測に合わせてあります。
  LoRA なし（1ショット）では長めに 1.5 倍ほどずれることがあります。
- 下げすぎると言い切る前に切れます。長い行や記号の多い行が途中で
  切れるときは上げてください。
- 上げると、LoRA によっては余った尺を埋めようとして語尾に無い言葉を
  発することがあります。どちらも LoRA ごとに癖が違うので、短い文で
  試してから使ってください。

---

## データの置き場所

すべて `APP` フォルダの中に入ります。フォルダごと移動できます。

| フォルダ | 中身 |
|-|-|
| `APP\voices` | 登録した参照音声 |
| `APP\loras` | 学習した LoRA |
| `APP\datasets` | データセット |
| `APP\lora_jobs` | 学習の記録 |
| `APP\outputs` | 生成した音声 |
| `APP\novels` | 青空文庫から取得したテキスト |

---

## 困ったとき

**ウィンドウが出ない**
`setup.bat` を最後まで実行したか確認してください。

**二重に起動しても大丈夫か**
問題ありません。アプリは起動時に空いている通信ポートを自分で探すため、
2つ目以降も別のポートで動きます。他のソフトを終了させる必要はありません。

**モデルが見つからないと出る**
`setup.bat` が最後まで通っていない可能性があります。
「セットアップが終わりました」と表示されたか確認してください。
`起動.bat` を使わずにアプリを直接起動した場合も出ることがあります。

**生成した声が参照音声に似ない**
学習データの質を見直してください。
無音・環境音・複数話者の混在が原因になりやすいです。

---

## ライセンス

コードは MIT ライセンスです（`LICENSE`）。
モデルの利用条件は配布元の規約に従ってください。

生成した音声の扱いは利用者の責任となります。
実在人物の声を本人の許可なく再現する用途には使わないでください。
